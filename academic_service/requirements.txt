# ============================================================================
# paper-service 依赖（运行期 + 测试统一管理，单一文件）
# 安装： python -m venv .venv && .venv/bin/pip install -r requirements.txt
# ============================================================================

# ---- 运行期依赖 ----
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
pyyaml>=6.0
requests>=2.31.0

# ---- 测试依赖（开发 / CI）----
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
